services:
  fastapi:
    build: ./fastapi
    depends_on:
        - mosquitto
        # db
    env_file:
        - ./config.env
  mosquitto:
    image: eclipse-mosquitto:latest
    ports:
        - "1883:1883"
    volumes:
      - ./mosquitto/config:/mosquitto/config:ro
  # db
  # arache superset